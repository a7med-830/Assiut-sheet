#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main() {
    int n, m;
    cin >> n >> m;

    char space[n][m];
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++)
            cin >> space[i][j];
    }

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {

        }
    }
}